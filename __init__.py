# utils package
# This file makes the `utils` directory a Python package so that its
# modules (pdf_parser, analyzer, recommendations) can be imported using
# `from utils.module_name import ...` inside app.py.
